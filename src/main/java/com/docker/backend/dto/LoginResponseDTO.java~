package com.docker.backend.dto;

public record LoginResponseDTO(String username, String password, String token) {
    public LoginResponseDTO(String username, String password, String token) {
        this.username = username;
        this.password = password;
        this.token = token;
    }
}
